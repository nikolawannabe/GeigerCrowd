@if(Request.IsAuthenticated) {
    <text>Welcome <b>@Html.ActionLink(@Context.User.Identity.Name,"Profile","Account")</b>!
    [ @Html.ActionLink("Log Off", "LogOff", "Account") ]</text>
}
else {
    @:[ @Html.ActionLink("Log On", "LogOn", "Account") ]
}
